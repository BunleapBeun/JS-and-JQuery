// operator

// +=, -=, /=, *=, %=, ++, --

let num = 10;
// num %= 10; // output: 0;

// +=, -=, /=, *=, %=, is the same each other, its get the default value to cal with new assign value
// for ++ (is add 1 to defalut value), and -- (is minus 1 with defualt value)

num++; // output: 11
num--;; // output: 9

console.log(num);

// if use with const

// const i = 10;
// i += 10; 
// console.log(i);

// it will be error cuz const can not reassign value