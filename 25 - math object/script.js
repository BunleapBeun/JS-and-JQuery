// math built-in object
// floor ceil sqrt min max random

var num = 12.343;

var result = Math.floor(num); // cut float
var result = Math.ceil(num); // add one, cut float
var result = Math.ceil(num); // add one, cut float
var result = Math.sqrt(num); // sqrt root
var result = Math.min(5, 10, 2, 45, 3); // get smallest value
var result = Math.max(5, 10, 2, 45, 3); // get largest value
var result = Math.random(100); // random num while refresh

console.log(result);
