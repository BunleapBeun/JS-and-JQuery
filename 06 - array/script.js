// array
// create array
// access array times, access array by index. index start from 0;

// array: can store multiple value; it can be store as: nested arr, string, number, null, boolean, function, object.

const names = ["John", "Leap", "Alex", 10, null, {salary: 1000}];

// object form
// {keyname : value}, this is we call object

names[2] = "Vis"; // why we can change value right here with const?
// ans: cuz we not change all value of array we just change only one item

// erorr with const

// names = 100; // error cuz we use with const, and const can not reassign value

console.log(names);
console.log(names[0]); // access to index 0




